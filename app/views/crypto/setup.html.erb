<h1>Train the model</h1>

<br/><br/>
Enter the training cutoff index<br/>
There are <%= @data_count %> total entries in the database.
<br/><br/>
<%= form_for @train_config, url: '/crypto/train' do |f| %>
  <%= f.text_field :volume %>
  <%= f.submit "Train the model" %>
<% end %>

